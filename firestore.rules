rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Alleen ingelogde gebruikers (anoniem of anders) mogen lezen/schrijven.
    // Pas familyId aan als je een vaste naam wilt afdwingen in de rules.
    match /families/{familyId}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
